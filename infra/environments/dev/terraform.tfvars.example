# Copy to terraform.tfvars (git-ignored) for local runs; CI passes these as TF_VAR_* instead.
project_id        = "my-gcp-project"
github_repository = "my-org/fiap-ford-spec-sync"
# domain          = "specsync.tubadev.com" # Default; set to "" to use only run.app.

google_oauth_client_id = "your-web-client.apps.googleusercontent.com"
